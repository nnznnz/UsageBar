# Build output
.build/
UsageBar*.app/

# SwiftPM — there should never be one of these (we have zero dependencies).
# If it appears, a dependency sneaked in. Investigate before committing.
Package.resolved

# macOS
.DS_Store

# Release/transfer archives
*.tar.gz
